<Window x:Class="WpfManagerApp1.Views.Windows.WorkInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfManagerApp1.Views.Windows"
        xmlns:vm="clr-namespace:WpfManagerApp1.ViewModel"
        mc:Ignorable="d"
        Title="WorkInfoWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" VerticalAlignment="Bottom"
                    Height=" 50"
                    Margin="25 0"
                    Orientation="Horizontal">
            <TextBlock Text="Тип задания: "/>
            <TextBlock Text="{Binding Path=CurrentWorkType}" />
        </StackPanel>
    </Grid>
</Window>
